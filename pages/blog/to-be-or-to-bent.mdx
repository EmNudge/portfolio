import { withRouter } from 'next/router'
import Layout from '../../components/layouts/Blog'

export const meta = {
  published: true,
  publishedAt: '2018-05-20',
  title: 'To Be Or To Ben\'t',
  summary: 'A wannabe linguist\'s take on words',
}

export default withRouter(({ children, router }) => (
  <Layout metaData={meta}>
    {children}
  </Layout>
))


>>To be or to ben't? That is the question. Whether 'tis nobler in the mind to suffer The slings and arrows of outrageous fortune, Or to take arms against a sea of troubles, And, by opposing, end them?

```js
const hello = "my friend loves dudes";
console.log(hello);
```

The more perspecacious of you might have noticed I took the liberty of slightly altering the first sentence of this famous quote from Shakespear's Hamlet.

